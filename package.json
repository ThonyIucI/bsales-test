{
  "name": "bsale-test",
  "version": "1.0.0",
  "main": "",
  "scripts": {
    "start": "npm start --prefix api",
    "install-api": "cd api && npm install && cd .. ",
    "heroku-postbuild": "npm run install-api"
  },
  "engines": {
    "node": "16.15.1"
  },
  "dependencies": {
    "morgan": "^1.10.0"
  }
}
